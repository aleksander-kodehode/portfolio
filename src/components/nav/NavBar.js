import { Nav } from "../../componentsStyled/navStyles";
import BurgerMenu from "./Burger";

const NavBar = () => {
  return (
    <Nav>
      <BurgerMenu />
    </Nav>
  );
};

export default NavBar;
